generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model InventoryModel {
  id       String @id
  sku      String @unique
  quantity Int
}
